import React from 'react';

import classes from './LogoSignature.module.css';

const logoSignature = () => (
    <div className={classes.Signature}>
            <span className={classes.Name}>Zachary Kopatch</span>
            <span className={classes.Title}>Front End Developer | Full Stack Engineer</span>
    </div>
);

export default logoSignature;